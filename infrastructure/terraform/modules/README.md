# Shared Terraform modules

Planned modules:

```text
identity
web
api
data
strava-ingestion
forecasting
notifications
ai-agentcore
observability-baseline
```

Modules are created only when their vertical slice is implemented. Each module must
include validation, least-privilege IAM, encryption, retention, cost controls, and a
short usage example.

