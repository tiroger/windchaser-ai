# JavaScript
node_modules/
.next/
out/
.turbo/
coverage/

# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfplan
tfplan
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Secrets and environment
.env
.env.*
!.env.example
*.pem
*.key

# Local data and generated artifacts
data/private/
artifacts/
tmp/

# Editors and OS
.DS_Store
.idea/
.vscode/


# Rotated Strava refresh token, written by scripts/fetch_strava_fixtures.py
.strava-refresh-token

# Generated fixture bundle. Contains real segment geometry near home and real PR
# times. Section 11 of the project plan requires sanitised fixtures and redacted
# home zones before any public demo, so the raw bundle stays local.
# Regenerate with: uv run python scripts/fetch_strava_fixtures.py
apps/web/fixtures/opportunities.json

# Vendored MapLibre worker bundle, synced from node_modules by predev/prebuild.
apps/web/public/maplibre-gl-*.mjs

# Effort history and derived calibration. Real ride timestamps, locations and
# power data. Regenerate with scripts/fetch_effort_history.py + build_calibration.py
data/training/
apps/web/fixtures/calibration.json

# Deployment package Terraform builds from the worker's source. Generated on
# every plan, so committing it adds a binary that changes with every edit and
# means nothing on its own.
.terraform-build/

# Local MCP client configuration. Names the app data bucket, which carries the
# account id, and this repository is public. .mcp.json.example is the shape.
.mcp.json
